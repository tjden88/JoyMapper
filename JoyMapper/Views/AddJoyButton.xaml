<Window x:Class="JoyMapper.Views.AddJoyButton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterOwner"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Нажмите кнопку на джойстике" Width="272" Height="140">
    <StackPanel Margin="10" VerticalAlignment="Top">
        <TextBlock Name="JoyNameText" HorizontalAlignment="Center" Text="Джойстик...." />
        <TextBlock Name="ButtonNumberText" HorizontalAlignment="Center" Text="Кнопка...." />
        <Button Content="Принять" Command="{Binding AcceptButtonCommand}" HorizontalAlignment="Center" Margin="10" Padding="20,2" />
    </StackPanel>
</Window>
